## Customize the test machine
machine:
  python:
    version: 2.7.3

## Customize dependencies
dependencies:
  pre:
    - pip install setuptools --no-use-wheel --upgrade

test:
  override:
    - py.test tests/pystanbol/